# CPSC5860_final_project

## Repo structure

[pics](./pics/) are result pictures generated from this project.

[code](./code/):
1. [graph_nn.py](./code/graph_nn.py) is used for graph structure built, train, test and evaluate. It handles the construction of the $k$-NN graph structure, training loops, and evaluation metrics for GCN and GAT
2. [mlp.py](./code/mlp.py) is used for basline MLP model to serve as a non-graph deep learning baseline.
3. [plot.py](./code/plot.py) is used for plot generating.
4. [simu.py](./code/simu.py) is used for simulated data since our data is not so balanced. It is specifically designed to address the 1:6.5 early-onset to late-onset ratio by generating balanced synthetic samples or managing stratified sampling
5. [xgb.py](./code/xgb.py) is used for xgboost baseline model. Also, [xgboost_baseline.ipynb](./code/xgboost_baseline.ipynbsx) These files demonstrate that non-linear tree-based models often outperform graph models in low-sample regimes.
6. [model_train.ipynb](./code/model_train.ipynb) is the main processing file.  This notebook orchestrates the entire processing workflow, including data loading, graph construction, and model execution.

Predicting early-onset breast cancer is challenging due to the complex interaction between high-penetrance rare variants and low-effect common variants[cite: 8]. Our research investigates whether **Graph Convolutional Networks (GCN)** and **Graph Attention Networks (GAT)** can capture non-linear patterns that traditional linear models or Polygenic Risk Scores (PRS) might miss[cite: 3, 10].

Despite the theoretical promise of "guilt-by-association" learning, our findings indicate that GNNs currently struggle with the high noise-to-signal ratio and extreme class imbalance inherent in genomic datasets[cite: 4, 6].

---


## Methodology & Model Analysis

The project implements the graph convolution operation defined as:
$$H^{(l+1)} = \sigma\left( \tilde{D}^{-\frac{1}{2}} \tilde{A} \tilde{D}^{-\frac{1}{2}} H^{(l)} W^{(l)} \right)$$

### Key Challenges Identified:
1. **Over-smoothing**: GNNs tend to average the representations of early-onset patients with their late-onset neighbors due to the lack of clear spatial clustering in the SNP spac.
2. **Low Homophily**: t-SNE projections show significant intermingling of classes, meaning edges in a $k$-NN graph often connect nodes with different labels.
3. **Sample Scale**: With only 74 early-onset cases in the TCGA-BRCA cohort, deep architectures like GNNs are prone to overfitting compared to regularized linear models.


## Results Summary

The refined methodology highlights a "learning plateau" for graph models:
- **XGBoost**: AUROC 0.674
- **GAT**: AUROC 0.595
- **GCN**: AUROC 0.547

While GAT shows marginal promise through its attention mechanism—which prioritizes informative neighbors—traditional models remain more robust for small-scale genomic stratification tasks.
